# CLAUDE.md

給日後維護者（人或 agent）的專案脈絡。**README.md 講「怎麼做」，這份講「為什麼這樣做」
以及「哪些地方不能亂動」。** 補資料的操作流程看 README，不重複。

## 專案定位

把不同地區的歷史放在**同一條時間軸**上並排比較。核心價值不是「有一個歷史年表」，
而是「秦統一中國那一年，羅馬正在打第一次布匿戰爭」這種**橫向對照**。

這句話是所有取捨的判準：**任何會讓橫向對照失真的優化，都不做。**

---

## 三條不變式（動到這些之前請先想清楚）

### 1. 縱軸位置必須忠實對應年份

畫面上 y 座標相同 = 年份相同，跨欄也成立。這是整個網站唯一的承諾。

具體衍生的規則：

- **時間刻度必須是線性的。** 曾考慮用非線性刻度處理「古代稀疏、近代密集」，
  否決了 —— 非線性之後兩欄的同一 y 仍然是同一年，但讀者對「這段距離代表多久」
  的直覺會壞掉，橫向對照就失去意義。密度問題改用**縮放 + importance 分層**解決。
- **標籤位移有硬上限。** 見下方「標籤排版」。
- **圖釘要以年份為中心**，不是以標籤列頂端對齊。曾經因為用 `top: labelY` 定位
  22px 高的標籤列，導致每個事件都比真實年份低了半列（11px）。現在
  `layout.ts` 產出的 `y` / `labelY` 一律是**中心**座標，`EventMark` 自己扣 `HALF_ROW`。

### 2. 顏色只承載「地區」這一個維度

事件類別（政治／戰爭／文化／科技／宗教／經濟）用**漢字圖釘**識別，不用顏色。

原因不是美感，是量測結果。六個類別的圖釘散佈在畫布上，任意兩個都可能相鄰
（屬於 all-pairs 情境，不是 stacked bar 那種只需相鄰安全的情境）。實測參考配色的
前六個 slot：

| 模式 | 最差色盲 ΔE | 最差正常視覺 ΔE |
|---|---|---|
| 亮色 | 3.2（門檻 8） | 12.9（門檻 15） |
| 暗色 | 1.6（門檻 8） | 10.6（門檻 15） |

**沒有任何六色組合能通過**，這不是換配色能解決的。地區只有兩個 slot（藍 `--r0`／
橘 `--r1`），all-pairs 全數通過（最差色盲 ΔE 24.7 亮／26.8 暗）。

漢字是比顏色更強的識別通道，而且不用查圖例。若日後真的想用顏色標類別，
**必須先把類別砍到三個以內**，或改成一次只顯示一個類別（facet）。

`--r0`…`--r7` 八個 slot 已定義在 `styles.css`，新增地區時 `RegionColumn` 會自動用
`r{slot % 8}`。

**地區色適用的是「相鄰配對」門檻**，不是 all-pairs —— 欄位是左右並排的，
相鄰欄才是真正會被拿來比對的一組，而且每欄都有常駐的 sticky 標題寫著地區名，
顏色不是唯一的識別通道。前四個 slot 實測（相鄰配對）：

| 模式 | 最差色盲 ΔE | 最差正常視覺 ΔE |
|---|---|---|
| 亮色 | 9.1（門檻 8） | 22.9（門檻 15） |
| 暗色 | 8.4（門檻 8） | 19.8（門檻 15） |

**超過四個地區時要重新驗證**，而且驗的是相鄰配對，所以 `regions.yaml` 的 `order`
會影響結果 —— 調換順序就得重驗。

**篩選不可以重新指派顏色。** `slot` 一律用地區在 `REGIONS` 裡的原始索引，
不是篩選後的名次。關掉一欄之後剩下的欄換色的話，讀者的顏色記憶就毀了。

### 2.5 傳說是獨立的一軸，不是第七個類別

三皇五帝、神武天皇、羅馬建城用 `legendary: true` 標記，**刻意不做成
`category: 神話`**。

- `category` 答「這是哪一種事」，`legendary` 答「這件事有多確定」，是兩個軸。
  伏羲畫八卦是 `culture`、黃帝敗蚩尤是 `war`、大禹治水是 `economy` ——
  全塞進「神話」就把這層丟掉了，而「神農教民耕種 vs 張騫通西域」正是這個
  網站要提供的比較。
- 「神話」是史學立場（等於斷言虛構，疑古／信古至今仍有爭議），
  「傳說」才是中性說法。
- `神` 會跟既有的 `教`（宗教）在界線上打架（天照大神算哪個？）。
- 旗標一樣能做成篩選 chip，不必是分類才能整組篩。

**這個旗標必須改變圖釘怎麼畫，不能只在詳情面板加註記。** 黃帝的前 2698
是晉人皇甫謐回推的，不是定年；把圖釘釘在精確的 y 上，本身就是在宣稱一個
資料撐不起的精度。所以是虛線圈 + 斜體年份 + 次要色標題 + 虛線引線，
掃一眼就看得出這條線比較「軟」。

弱化**不用整體 `opacity`**，那會把文字一起弄糊。也刻意不覆蓋 `imp-5` 的
`font-weight` —— 重要度是另一個軸，重要的傳說事件本來就該比次要的顯眼。

### 3. 同一 track 上的時期不可重疊

`data.ts` 的 `assertNoOverlap` 會在載入期直接 throw。這不是潔癖 —— 時期是畫成
背景色帶（面），重疊會互相蓋掉，且語意上也講不通。

歐洲不是單一政體，所以拆成兩條 track：`0` 希臘／拜占庭一線，`1` 羅馬／西歐一線。

這裡曾經寫著「未來加中東、印度時多半也會需要多 track」。**後來加了，而且刻意
沒有加 track**，理由見下一節。

---

## 「世界」欄：一欄容納四個文明

第四欄原本是 `europe`，2026-08 改名為 `global`，範圍重新定義為
**台／日／中以外的全世界**，事件補入美洲、中東、南亞（101 → 192 則）。

改名前這一欄就已經名不副實：`eu-crucifixion` 在黎凡特、`eu-al-andalus` 在伊比利、
`eu-columbus` 在美洲、`eu-constantinople-fall` 在安納托利亞。

### 既有 `eu-*` 事件 id 一律不動

地區 id 不進網址，資料夾改名零成本；**但事件 id 會進**（`#e=eu-caesar`）。
所以 `eu-` 前綴留著，新事件才用 `am-`／`me-`／`sa-`。看到一欄裡混著四種前綴
不要「順手統一」，那會讓所有既有的分享連結失效。

`data.ts` 只用 `assertUniqueIds` 管全域唯一，不管前綴規則。

### 斷代只畫歐洲兩條 track，這是刻意的

這一欄裝了四個文明，卻只有希臘／羅馬兩條色帶 —— 美洲、中東、南亞的事件
沒有任何背景可以對照。**看到這個不要急著加 track。**

- 斷代在這裡的作用是**尺規**，不是覆蓋率。讀者要的是「阿育王皈依那年，
  羅馬還在共和」，歐洲兩線已經足夠當那把尺。
- 每條 track 是固定的 26px（`--rail-w`）+ 2px 間隙，直接從 340px 的
  `MIN_COL_W` 裡扣。兩條剩約 280px 給標籤，四條只剩約 224px，標題截斷會明顯變多。
- 真要加，先確認那條 track 從頭到尾都填得滿。近東線（蘇美—波斯—哈里發—鄂圖曼）
  是唯一接近連續的候選；南亞、美洲都會出現大段空白，而空白色帶比沒有色帶更難讀。

`-3000`…`-1601` 兩條 track 都沒有色帶，是既有的空窗，不是這次改壞的。

### 子區靠標題自然帶地名，不做 `subregion` 欄位

考慮過加 `subregion` 欄位 + 工具列篩選 chip，否決了。多數歷史事件的標題本來
就含地名或專名（「阿育王皈依佛教」「西班牙征服印加帝國」「阿拔斯王朝取代倭馬亞」），
辨識通道已經存在。加欄位要動 `schema.ts`／`data.ts`／`Toolbar`／`DetailPanel`
四支檔案，而且**只有這一欄用得到** —— 台日中三欄永遠是空的。

也否決了「〔南亞〕…」這種標題前綴：340px 欄寬下每則吃掉四個字，
而且與其他三欄的標題風格不一致。

### 欄位範圍變大時，`importance` 尺規要重新對齊

改名前歐洲欄 101 則裡有 **36 則 imp-5（36%）**，是其他欄的兩三倍 ——
那是在「這一欄只裝歐洲」的前提下給的。範圍擴大成四個文明後，
全域視角（`TIERS` 只顯示 imp-5）那一層會直接爆掉。

處理方式是兩邊一起收：新事件 imp-5 只給 11 則，同時把 10 則「歐洲區域級
而非世界史級」的既有事件從 5 降到 4（凱撒遇刺、諾曼征服、大憲章、
克里斯提尼改革…）。結果全欄 imp-5 = 37，與中國欄的 37 持平；
實測全域視角讀得到的標題 20 則，比中國欄的 18 還多。

**這件事跟「圖釘太多」不是同一個問題，別混用同一個解法。**
密度問題的槓桿是 `topic.yaml` 的 `defaultPpy`（見「標籤排版」那節的實測表），
調 importance 沒有用。這裡動 importance 的理由是**尺規本身的定義變了** ——
同一欄裡「最高一級」代表的意思，從「歐洲史上最重要」變成「世界史上最重要」。

日後若再擴大任何一欄的範圍（例如把非洲、東南亞也併進來），
要回頭做同一件事，判準是：**全域視角下這一欄的 imp-5 則數，
不該明顯超過其他欄。**

---

## 檔案地圖

```
src/lib/schema.ts       Zod schema（類別、主題、事件、時期的形狀）
src/lib/topic.ts        從網址推出當前主題 ★ 所有資料載入的源頭
src/lib/data.ts         glob 掃 YAML → 挑出當前主題 → 驗證 →
                        匯出 TOPIC / REGIONS / TIMELINE / CATEGORIES
src/lib/scale.ts        年↔像素、刻度級距、importance 分層門檻
src/lib/layout.ts       標籤排版演算法 ★ 最需要小心的一支
src/components/         Axis / RegionColumn / PeriodRail / EventMark / Toolbar /
                        DetailPanel / HelpOverlay / ThemeToggle
src/App.tsx             狀態、縮放錨定、時間游標、欄數計算
vite.config.ts          base 推導 + 依 topic.yaml 產生各主題的 HTML entry
index.html              所有主題共用的模板（title/description 只是預留位）

src/topics/<主題>/
  topic.yaml            名稱、columnLabel、defaultPpy、jumps、root
  regions.yaml          欄位定義（order 決定左右順序與配色 slot）
  timeline.yaml         時間軸的上下界
  categories.yaml       類別（選填，沒有就用 schema.ts 的預設六類）
  <欄位>/events.yaml
  <欄位>/periods.yaml
```

資料是 `import.meta.glob('../topics/*/*/events.yaml')` 掃進來的，
**新增主題或欄位都不需要改任何程式碼**，只要放檔案。

### 主題是 per-document 的常數，不要改成 state

每個主題有自己的 HTML entry（`/aoe/` 與 `/aoe/tw-railway/` 是兩份不同的
index.html），所以瀏覽器一載入，「現在在看哪個主題」就已經定了。

這是多主題能做得很輕的關鍵：`MIN_YEAR`、`REGIONS`、`CATEGORIES` 全部維持
**模組層常數**，只是值改由當前主題決定。`layout.ts`、`urlState.ts` 與所有
繪圖元件因此一行都沒動。

**不要為了「主題可以即時切換」把它改成 state。** 那會讓 `scale.ts` 的
`MIN_YEAR / MAX_YEAR / SPAN_YEARS` 變成函式參數，一路擴散到每一支檔案。
換主題就整頁重新載入，成本比那個改動低太多。

### 為什麼每個主題要有實體的 index.html

**GitHub Pages 沒有 server-side rewrite。** `/aoe/tw-railway/` 這個路徑上
必須真的存在一份 HTML，否則直接 404 —— 前端 router 根本沒有機會執行。

非 root 主題的那份是 `vite.config.ts` 從根目錄的 `index.html` 複製產生的
（gitignored）。**不要改成手寫**：`index.html` 的 `<head>` 有一段防閃爍的
theme inline script，key 必須跟 `theme.ts` 的 `THEME_KEY` 一致，抄成 N 份之後
漏改不會報錯，只會在暗色系統上閃一下白底。

`<title>` 與 meta description 一律由 `topic.yaml` 決定，**根目錄那份也一樣**
（走 `transformIndexHtml`）。曾經是產生子目錄 HTML 時做字串抽換，那樣 root 主題
的標題就留在 index.html 裡，變成兩套規則。

### 跨主題的東西要留意「這個值是為誰調的」

好幾個常數原本是為五千年跨度的世界史挑的，換一個 141 年的主題就全錯：

| 原本 | 問題 | 現在 |
|---|---|---|
| `MIN_PPY = 0.15` | 1885–2026 整條軸只有 21px 高 | `700 / SPAN_YEARS` |
| 開場 `ppy = 1` | 141 年全擠在 141px | `topic.yaml` 的 `defaultPpy` |
| `JUMPS = [-2000…1950]` | 按鈕全部出界，按了像沒反應 | `topic.yaml` 的 `jumps` |
| 六個類別寫死在 schema | 鐵道史的「通車」「廢線」都變成「科」 | `categories.yaml` |

`TIERS`（importance 分層）**刻意維持絕對值**。改成相對於 `MIN_PPY` 試過，
反而更糟：跨度短的主題 `MIN_PPY` 高，相對門檻換算回去會超過 `MAX_PPY`，
低重要度的事件永遠出不來。維持絕對值的話，跨度短 → ppy 高 → 事件自然全部顯示，
而那正是資料少的主題該有的行為。

**沒有欄位資料的 UI 就整個不顯示，不要留一顆按了沒反應的開關。**
鐵道史沒有傳說事件，所以工具列的「傳說」chip 與說明裡那一格都不畫。
說明覆蓋層的範例事件也是從當前主題的真實資料挑的 —— 寫死的話，
「秦滅六國」會出現在鐵道史的說明裡。

### 時間軸的上下界（`timeline.yaml`）

`minYear` / `maxYear` 曾經寫死在 `scale.ts`。改成資料有兩個原因：

**寫死的值會安靜地過期。** `MAX_YEAR` 原本是 2025，跨年之後軸的下緣就停在去年了 ——
`ticks()` 的迴圈是 `y <= MAX_YEAR`，連當年的刻度都不會產生。

**超出範圍的資料會靜默消失。** 這是更嚴重的一個。`yearToY` 對範圍外的年份就是算出
負的 y，元素被畫到畫布上方，`.lane-body` 又是 `overflow: visible`，於是：不報錯、
主控台乾淨、欄位標題的「N 則」還照算，只有讀者永遠找不到那一筆。實測一則
`year: -5000` 的事件會落在 `top: -1211px`。

所以 `data.ts` 有 `assertInRange`，事件（含 `endYear`）與時期（`start`…`end`）
都會檢查。跟 `assertNoOverlap` 同一種精神：**寧可載入期整片白配一則明確訊息，
也不要靜默掉資料。**

**為什麼是設定值，不是從資料推導。** 推導不會過期，聽起來更好，但一則離群的資料
（例如 -10000）就會把整條軸拉長，其他五千年全被擠扁 —— 而擠扁的正是拿來做橫向
對照的那段。範圍固定，`SPAN_YEARS`、刻度級距、捲動位置才穩定。代價是要記得維護，
那就是 `assertInRange` 在幫忙盯的事。

擴大範圍時記得：`SPAN_YEARS` 一變，整條軸的像素高度與捲動座標都會跟著變。

**`minYear = -3000` 是刻意維持的，不要因為看到資料被截就往前調。**
`繩文` 的 `note` 寫著「實際上遠早於西元前 3000 年，此處為時間軸起點所截」，
台灣的 `大坌坑文化`、中東的 `蘇美`／`埃及第一王朝` 也一樣卡在起點 ——
這些提示很容易讓人想把軸拉長。

**先搞清楚成本落在哪裡。** `yearToY` 是 `(year - MIN_YEAR) * ppy`，
所以**固定 ppy 下，任兩年之間的像素距離與 `MIN_YEAR` 無關** ——
拉長軸只是換原點、把畫布變高，預設視角的疏密一格都不會變。
會變的只有 `MIN_PPY = 700 / SPAN_YEARS`，也就是**縮到最小、整條軸一屏**那一端：

| minYear | 跨度 | 全域視角下「前500…西元500」的高度 |
|---|---|---|
| **-3000（現況）** | 5026 | 139px（100%） |
| -4000 | 6026 | 116px（83%） |
| -6000 | 8026 | 87px（63%） |
| -10000 | 12026 | 58px（42%） |

（這段原本寫成「同一個 ppy 下所有內容壓縮到 63%」，那是錯的，會讓人以為
延長的代價遍及所有視角。63% 只發生在全域視角。）

**真正的理由是填充率，不是壓縮。** 前3000–前2501 只有 12 則、
前2500–前2001 只有 8 則 —— 是整條軸最稀的一格。預設 `ppy=1` 下，
前3000–前2000 是 1000px 的畫布裝 20 則，對照西元 1500–1600 是 100px 裝 51 則，
**密度差 25 倍**。軸的頂端已經是一大片空白，往前延只是把空白加在最空的那一端。

### 卡在起點的事件有數量上限

超出下界的資料一律「夾到 `minYear` + 在 `desc` 註明實際年代」
（`埃及第一王朝` 實際約前 3100、`蘇美` 約前 3400、`繩文` 約前 14000）。

**但這個做法有天花板，而且第二則就已經踩到了。** 目前 -3000 那條線上有 5 則
（台灣 1、中國 2、世界 2）。

實測世界欄那兩則：`蘇美` 先佔到夾住後的位置（`y = HALF_ROW = 13`），
`埃及第一王朝` 只能排到 `y = 39`，相對它自己的真實 `y = 0` 位移 39px，
**超過 `MAX_SHIFT`（ppy=1 時是 30px），於是在預設視角退化成只剩圖釘**，
放大到 ppy≈1.5 以上才讀得到標題。兩則同為 importance 5，誰勝出純粹看
檔案順序 —— 穩定，但也就是任意。

這是可接受的（CLAUDE.md 一貫的取捨就是「寧可退化成圖釘，放大就讀得到」），
但它說明**第三則開始只會更糟**：位移繼續累加，而且是疊在一個本來就虛構的
年份上。**每欄至多兩則，然後停手。**

再有需求時，正解是**另開一個主題**而不是延長這條軸。新石器革命
（農業起源約前 9500、哥貝克力石陣約前 9600、耶利哥約前 8000、
恰塔霍裕克約前 7500、賈湖約前 7000、梅赫爾格爾約前 7000）本身是很好的
橫向對照題材，但它需要 `minYear` 約 -12000，會吃掉現有比較窗口的一半以上，
而且類別（農業／聚落／工具／藝術）跟政治／戰爭／宗教根本不是同一組。
開一個 `prehistory` 主題，自己的 `timeline.yaml`、`defaultPpy`、`categories.yaml`，
成本是一個資料夾的 YAML，對世界史這條軸零影響。

另外，落在 `minYear` 上的事件標籤會被切掉上半列 —— 那是排版問題，已在
`layout.ts` 夾住，**不是調 `minYear` 的理由**（見「實作過程中踩到的坑」第 7 條）。

---

## 標籤排版（`layout.ts`）—— 最容易改壞的地方

### 問題

時間相近的事件，標籤（22px 高）會重疊。直覺解法是「往下推擠」。

### 為什麼單純往下推是錯的

第一版就是這樣做的，`MAX_SHIFT = 150`。結果：歐洲欄在西元前 500～西元 30 之間
事件密集，位移**沿時間累積**，最後把**凱撒遇刺（前 44）排到了光武中興（25）下方**。

橫向對照直接失真，而且錯得很隱蔽 —— 畫面看起來很整齊，只是年代是錯的。

### 現在的做法

1. 依**重要度**（不是年份）決定配置順序，同級才比年份
2. 每則事件挑位移最小的標籤欄（`MAX_LANES = 2`，寬螢幕才有）
3. 位移超過上限 → **退化成只畫圖釘**，標籤不畫

被推開的標籤一律畫**引線**指回真實年份，引線頂端那一橫才是真正的位置。

### 位移上限的單位是「年」，不是像素

`MAX_SHIFT_YEARS = 30`，再乘上 `ppy` 換算成像素（夾在 14–56px）。

**不可以改回寫死像素。** 同樣 44px，在 0.6 px/年 的全域視角代表 73 年，
在 5 px/年 只代表 9 年 —— 縮得越遠誤差越大，偏偏那正是拿來做跨區對照的視角。
實測寫死 44px 時，同為西元前 300 年的歐幾里得與彌生文化會被畫成相距 43px，
視覺上差了 72 年。改成以年為單位後，跨欄順序顛倒從 21 處降到 5 處，
最嚴重的偏差從 43px（72 年）降到 15px（25 年）。

### 配置順序必須是重要度優先

照年份先到先得的話，一個重要度 3 的事件會把十年後的重要度 5 擠成圖釘。
實測前 202 年劉邦稱帝、前 138 年張騫出使西域就是這樣掉的 —— 畫面看起來沒問題，
但被犧牲的是最該留下的那些。

演算法因此不是單純的「往下推」，而是**區間佔位**（`firstFreeBelow`）：
高重要度的先佔位，低的往空隙填，填不進去才變圖釘。輸出時再排回年份順序，
渲染結果才穩定。

### 調參時的注意事項

- `MAX_SHIFT_YEARS` 只能往小調，不能往大。往大調會重現順序失真。
- `MAX_LANES` 上調到 3 試過，視線要之字形來回掃，而且 340px 的欄寬會截斷標題。
  維持 2。寧可退化成圖釘（放大就讀得到），也不要三欄。
- 覺得「圖釘太多、資訊太少」時，**解法是調該主題 `topic.yaml` 的 `defaultPpy`，
  不是門檻也不是資料。** 這條曾經寫成「調 `minImportance()` 門檻或修資料的
  importance」，實測兩者都錯（下面的數字是世界史那份量的）：

  | 預設視角的門檻 | 顯示 | 圖釘 | **讀得到標題** |
  |---|---|---|---|
  | 5+（更嚴） | 91 | 27（30%） | **64** |
  | 4+（現況） | 226 | 114（50%） | **112** |
  | 3+（更鬆） | 322 | 183（57%） | **139** |

  調嚴之後圖釘「比例」確實好看，但讀得到的標題少了將近一半 ——
  **圖釘比例是會騙人的指標，分母跟著變。要看的是讀得到幾則。**

  修資料的 importance 也沒用。台灣 1900 年後有 25 則在 4 級以上（含 7 則 5 級，
  降不得），而 ppy 0.6 時那 120 年只有 72px，一列標籤要 26px ——
  **最多塞得下 2–3 個，再怎麼降級都塞不進去。** 實測把 11 則從 4 降到 3，
  台灣讀得到的標題仍然是 9 則，只是那 11 則從圖釘變成完全不顯示。

  有效的是給每則事件更多垂直空間，也就是開場的 `ppy`：

  | 開場 ppy | 門檻 | 圖釘 | **讀得到** | 一屏年數 |
  |---|---|---|---|---|
  | 0.6（舊） | 4+ | 114（50%） | 112 | 1167 |
  | 1.0（現況） | 4+ | 71（31%） | **155** | 700 |

  門檻邏輯完全沒動（`< 1.2` 都是 4+，同樣 226 則），純粹是把同一批事件攤開。

---

## 多地區版面（橫向捲動 + 凍結年代軸）

地區數量沒有上限。版面規則：

- 地區少 → 平分寬度，跟以前一樣一個畫面看完
- 地區多到每欄低於 `MIN_COL_W`（340px）→ 畫布橫向捲動，每欄保住最小寬度
- 年代軸 `position: sticky; left: 0`，橫向捲動時仍讀得到年份
- 每欄的標題 `.lane-head-name` 也是 sticky-left，卡在年代軸右緣，
  欄位捲到一半時仍看得到自己在哪一區

### 畫布寬度是在 JS 算的，不是靠 CSS

`App.tsx` 算出 `canvasWidth` 直接寫進 `.lanes` 的 style。**不要改成
`width: max-content` 讓 CSS 自己撐**：格線與時間游標用 `inset-inline: 0` 跨滿整條，
它們的參考寬度就是 `.lanes`；若 `.lanes` 只有一個畫面寬，捲動後右半邊就沒有格線了。
而 `max-content` 會讓欄寬被最長的標題撐開，失去等寬。

### z-index 階梯（改動前先看這裡）

```
0  格線
2  事件圖釘與標籤
4  欄位標題（sticky top）
6  年代軸（sticky left，自成 stacking context，蓋過內容與其他標題）
7  時間游標 —— 必須高於年代軸，年份讀數才不會被軸蓋掉
```

時間游標的年份讀數是 `position: sticky; left`，黏在畫面左緣不隨橫向捲動跑掉。

### sticky 的陷阱

`.lane-head-name` 必須 `align-self: flex-start`。被 flex 拉成滿欄寬的話，
元素沒有可滑動的餘裕（sticky 不能超出 containing block），`position: sticky`
會完全沒有效果 —— 而且不會報錯，只是安靜地失效。

## 補出處的流程與陷阱

`sources` 欄位是給讀者查證用的指標。**加了出處不等於可以照抄維基的文字** ——
條目文字是 CC BY-SA（含相同方式分享），抄了整批資料就得跟著改授權。
年代與事實本身不受著作權保護，所以本專案一律是「查維基確認年代，
`title` 與 `desc` 自行撰寫」。授權界線的完整說明在 README。

### 可以引用哪些維基站台

**不限中文維基。** 日本史可引日文版、歐洲史可引英文版 —— 那是該主題的母語社群，
條目通常遠比中文版完整。實測中文版的單薄程度：`堂島米會所` 1.3KB、
`呂克昂` 1.1KB、`羅斯受洗` 1.3KB，日／英文版分別是 23KB、20KB、31KB。
有些主題中文版根本沒有條目（`拜占庭聖像破壞運動` 只有消歧義頁列出的紅連）。

補美洲／中東／南亞時中文版的缺口更大，有些條目只有英文版有
（`Lei Áurea` 巴西廢奴法、`Arab conquest of Sindh` 阿拉伯征服信德、
`Classic Maya collapse` 馬雅古典期崩潰）。

規則：

- **中文版夠用就用中文版**，讀者查證最方便。母語版是拿來補不足的，不是預設。
- 中文版不存在，或明顯只是個小條目時，**額外掛上母語版**（不必刪掉中文版那筆）。
- **`title` 一律寫繁體中文**，標明語言：`日文維基百科：伴天連追放令`、
  `英文維基百科：君士坦丁堡之圍（674–678）`。URL 才是該站台的原文標題。
  讀者在詳情面板看到的是標題，先讓他知道點進去會是什麼語言。

### 正確的流程：先驗證，再寫入

第一批（台灣）是憑記憶寫完再驗證，61 個網址錯了 4 個，回頭修很費事。
後面幾批改成先用維基 API 批次查標題，錯誤在寫入前就攔掉：

```bash
# 一個請求最多 50 個標題，比逐一發 HTTP 請求快得多。查哪個站台就打哪個 api.php
https://zh.wikipedia.org/w/api.php?action=query&format=json&redirects=1
  &converttitles=zh-hans&prop=pageprops&ppprop=disambiguation&titles=A|B|C
```

回應裡 `missing` 表示不存在，`redirects` 表示被導向何處，
`pageprops.disambiguation` 表示那是消歧義頁。**三者都要看。**

寫完之後 `npm run lint:data -- --check-pages` 會把同樣的檢查跑一遍，
按站台分組查詢，是這三個陷阱的自動化版本。

### 陷阱一：API 回報 missing 不代表真的不存在

**中文維基 API 的 `titles=` 參數不做繁簡變體轉換，但 `/wiki/` 路徑會做。**
所以繁體標題常被誤報為 missing，實際上網址完全正常。
歐洲那批 16 個回報 missing 只有 7 個是真的；中國那批 3 個全是誤報。

**加上 `converttitles=zh-hans` 就沒有這個問題了**（只對中文維基需要）。
在知道這個參數之前，唯一的辦法是 missing 之後再逐一測 URL。

### 陷阱二：連得通不代表指對地方（`--check-urls` 抓不到）

這類最危險：HTTP 200，但條目的範圍不對。**站台變多之後更容易踩到。**

| 寫的 | 實際是什麼 | 問題 |
|---|---|---|
| `鎖國` | 重新導向到孤立主義 | 變成談通用概念，不是江戶幕府的鎖國政策 |
| `清兵入關` | 重新導向到一片石之戰 | 只剩山海關一役，涵蓋不了 1644 整件事 |
| `米蘭敕令` | 消歧義頁 | 君士坦丁的和拿破崙的都叫這個名字 |
| `君士坦丁一世` | 消歧義頁 | 還有一位蘇格蘭國王同名 |
| `聖像破壞運動` | 消歧義頁 | 拜占庭的與喀爾文派的都叫這個名字 |
| `權利法案` | 泛稱概念條目 | 談的是「權利法案」這種東西，不是 1689 年英國那部 |
| `薩爾貢` | 消歧義頁 | 阿卡德的與亞述的同名；`薩爾貢一世` 更糟，那是亞述那位 |
| `帕尼帕特戰役` | 消歧義頁 | 同地打過三場，要的是 `第一次帕尼帕特之戰` |

下半六筆是後來用 `--check-pages` 掃出來的，**在資料裡躺了很久**：
`--check-urls` 只驗狀態碼，消歧義頁一樣回 200。

最後兩筆是補世界史那批時抓到的，**都是人名／地名同名**。這類特別陰險：
挑一個「看起來更精確」的替代標題（`薩爾貢` → `薩爾貢一世`）反而會指到別人身上。
補古代史時同名的君主特別多，換標題之後一定要回頭跑一次 `--check-pages`。

消歧義頁現在有自動檢查了；**「泛稱概念」那類還是只能靠人看**
（`權利法案` 是一篇正常條目，API 無從分辨）。判準是：條目談的是不是
「這一件事」，而不是「這一類事」。重新導向若只是繁簡轉換
（`鎌倉幕府 → 镰仓幕府`）可以忽略，換了詞的要停下來看。

### 陷阱三：標題裡的破折號

`英西戰爭 (1585年—1604年)` 用的是破折號 `—`，不是半形連字號 `-`。
肉眼幾乎看不出差別，但寫錯就是 404。同類的還有間隔號 `·`（`馬丁·路德`），
以及英文條目的 en dash：`Siege of Constantinople (674–678)` 是 `–` 不是 `-`。

### 為什麼繁體標題優先（指中文維基的條目）

繁簡重新導向很穩定，不會被刪。繁體標題對本站讀者更好讀，
所以除非重新導向指到錯的地方，一律保留繁體形式。

URL 裡的空白一律寫成底線（`羅馬之劫_(410年)`、`Sack_of_Rome_(410)`）。
字面空白瀏覽器雖然吃得下去，但同一份檔案裡兩種寫法混用沒有意義。

## JS 與 CSS 之間手動同步的常數

沒有共用來源，改一邊要記得改另一邊：

| JS | CSS | 值 |
|---|---|---|
| `App.tsx` `HEAD_H` | `--head-h` | 58px |
| `App.tsx` `AXIS_W` | `--axis-w` | 88px |
| — | `.lane-head-name` 的 `left` 偏移 | `calc(var(--axis-w) + 10px)` |
| `layout.ts` `ROW_HEIGHT` / `EventMark.tsx` `HALF_ROW` | `.mark` height | 26 / 11 / 22px |

`HEAD_H` 錯了會讓縮放錨定與時間游標整體偏移（sticky header 佔了捲動座標的前 58px）。

還有一個跨檔案的字串常數：`theme.ts` 的 `THEME_KEY`（`aoe:theme`）與 `index.html`
`<head>` 裡那段防閃爍的 inline script **必須一致**。那段 script 的用途是在首次繪製前
就套用存下來的主題，不然手動選亮色的人在暗色系統上會先閃一下暗底 —— React 掛載
太晚了。改 key 的時候兩邊要一起改。

---

## 詳情面板是停靠的，不是浮動的

`.detail` 曾經是 `position: fixed` 的浮動卡片，實測代價很高：蓋掉最右欄
**79% 的寬度、15 則事件**，而且**連剛點的那一則都看不到** —— 點最右欄的
事件等於讓它自己消失。

改成 `.workspace` 底下的 flex 兄弟節點：畫布跟著變窄，完全不遮擋。
欄寬會自動重算，因為 `viewportWidth` 是 `ResizeObserver` 量 `.scroller`
的 `clientWidth` 得來的，不是 `window.innerWidth`。實測開面板後
被遮蔽的事件從 20 則降到 **0**。

**縱軸位置與寬度無關**（y 是年份算的），所以開關面板不會讓任何事件上下位移，
第一條不變式安全。

**`.workspace` 的 `min-height: 0` 與 `.scroller` 的 `min-width: 0` 不能拿掉。**
flex item 的預設 `min-size` 是 `auto`，不給 0 的話 `.scroller` 不會收縮，
內部也不會自己捲動 —— 而且不會報錯，只是安靜地不動。

**已知代價：欄寬變窄可能讓 `laneCount` 從 2 掉到 1，標籤退化成圖釘。**
發生條件是「地區少 + 螢幕寬」（例如 2 區 @1568px：740px → 560px）。
4 區的常見情境不會觸發（454 → 364，都是 1 欄），實測標籤數 265 → 265 沒有損失。
可逆，關掉面板就復原，所以不另外處理。

窄螢幕（`max-width: 1099px`）沒有並排的空間，改成貼底的抽屜。

---

## 搜尋（`search.ts` / `SearchBox.tsx`）

**搜尋是導覽，不是篩選。** 曾考慮讓搜尋直接濾掉不符的事件，否決了 ——
橫向對照需要各區的事件都留在原位，而且符合的結果散佈在五千年裡，
篩完只會得到一條到處是空洞的時間軸。所以搜尋只產生清單，點了跳過去。

比對標題／年份／描述／地區／類別，依命中欄位加權排序。中文不做斷詞，
直接子字串比對 —— 使用者輸入的多半是「鴉片」「馬關」這種詞根，斷詞容易切錯。

**跳轉前一定要先確保目標畫得出來。** 重要度低於當前層級、地區或類別被關掉、
傳說被關掉，任何一項成立都會讓使用者跳過去只看到空白，而且畫面上沒有任何線索
說明為什麼。`revealEvent` 會把這四項都補上，需要放大時借用縮放錨定的 `anchorRef`。

反查「要放大到多少才看得到」用 `scale.ts` 的 `ppyForImportance`。
**它與 `minImportance` 共用同一張 `TIERS` 表** —— 正反查各寫一份門檻遲早會走鐘。

一個順帶修掉的缺陷：**圖釘（`dotOnly`）原本被選中時沒有任何視覺回饋**，
只有帶標籤的 `.mark` 有 `is-selected`。搜尋跳到密集區時常落在圖釘上，
沒有回饋就變成「跳過去卻看不出跳到哪」。現在圖釘會放大並加外環。

---

## URL 狀態（`urlState.ts`）

`#y=-221&z=2&e=cn-qin-unification` —— 年份、`ppy`、選中的事件 id。

**`VIEW_ANCHOR`（0.4）必須三處共用**：`scrollToYear`、開場定位、網址讀寫。
在此之前跳年代用 0.4、開場用 0.45，兩個不一致 —— 只要有一處不同，
分享出去的連結打開後位置就會偏，而且偏多少還跟對方的視窗高度有關。

**一律 `replaceState`，不用 `pushState`。** 捲動是連續動作，每捲一下塞一筆
歷史紀錄的話，返回鍵就廢了。實測捲 8 次、縮放 2 次、選取 1 次，`history.length`
增加 0。

**捲動時的寫入一定要 debounce（250ms）。** 捲動事件一秒可觸發數十次，
每次都呼叫 `replaceState` 會拖慢捲動，Safari 還會丟「呼叫太頻繁」的警告。
縮放與選取是離散動作，不需要。

副作用是開場的程式化捲動也會觸發一次，所以網址在載入後約 0.25 秒就會自己補上。
這是刻意接受的：**網址永遠反映現況，「複製網址列」才會一直是可靠的分享方式。**

**篩選狀態刻意不放進網址。** 分享時對方該看到完整的四個地區；若把「我關掉了
日本欄」也傳過去，對方只會覺得資料不見了。網址表達的是「看哪裡」，
不是「我調過哪些開關」。

外部改動 hash（貼上別人的連結、手動編輯網址）由 `hashchange` 處理；
自己寫入用 `replaceState` 不會觸發它，所以不會打架。若 `ppy` 也變了，
借用縮放錨定的 `anchorRef` 讓 layout effect 去定位，不要自己算 —— 那條路徑
已經處理過「先換 ppy 再對齊年份」的順序問題。

---

## 說明覆蓋層（`HelpOverlay.tsx`）

標題列的「？說明」開啟，Esc／點背景／關閉鈕都能關掉，焦點會還給觸發的按鈕。

**圖例裡的範例是用真正的 class 畫的**（`.mark`、`.leader`、`.span`、
`.mark-dot-only`、`.imp-4/5`、`.is-legendary`），不是另外畫一套示意圖或截圖。
改了 `styles.css`，說明會跟著變。**不要為了排版方便把它改成手繪 SVG 或圖片** ——
那份複製品一定會悄悄與實際畫面脫節，而說明錯了比沒有說明更糟。

範例外面包一層 `.help-sample.r0`：`.r0` 是為了給 `--region`（圖釘靠那個變數上色），
`.help-sample` 是絕對定位的定位基準。位置一律由元件的 `top` prop 明確指定，
不要用 CSS 的 `nth-of-type` 猜 —— 引線那條範例就是靠 `top` 錯開才示範得出
「標籤被推開」。

**範例用的事件也是從當前主題的真實資料挑的**（依重要度取最高的那則），
理由跟「用真正的 class 畫」是同一個：寫死的話，世界史的「秦滅六國」會出現在
鐵道史的說明裡。挑不到就整格不顯示 —— 沒有傳說事件的主題本來就不該有一格
在教你怎麼讀傳說，沒有跨度事件的主題也不該有那一格。

**這裡有一份不會自動同步的東西：說明文字。** 它複述了縮放分層、位移上限、
傳說旗標、出處政策這些行為。改動 `minImportance()` 的門檻、`MAX_SHIFT_YEARS`、
`legendary` 的視覺，或出處規則時，記得回來看一眼這支檔案。

「如何填寫資料」刻意**不**寫進站上，只放一個連到儲存庫的連結 ——
README 已經有完整的一份，站上再寫一份就是兩份會各自腐化的文件。

---

## 其他決策紀錄

**Vite + React + TS，不用 Astro／Hugo。** 這是縮放、平移、篩選的互動畫布，
不是內容頁，SSG 的零 JS 優勢用不上。

**自己刻，不用 vis-timeline／TimelineJS。** 前者對西元前年份支援很差，
後者是單一敘事線做不了並排比較。核心其實只是一個 `年 → 像素` 的比例尺。

**年份用 signed integer，不用 `Date`。** `Date` 處理西元前很糟。
另外**沒有西元 0 年**（-1 的下一年是 1），schema 有 refine 擋。
中國年號、日本和曆日後若要支援，一律當**顯示用註記**，正規化儲存永遠是西元。

**時期（面）與事件（點）分開存。** 這是可讀性的分水嶺 —— 朝代畫成背景色帶，
事件畫成圖釘。混在一起畫會讓兩者都讀不清楚。

**DOM 渲染，不用 Canvas。** 這個量級的事件數 DOM 完全撐得住，而且免費得到
文字選取、無障礙、CSS 主題。**超過約 2000 則再考慮虛擬化**（`RegionColumn` 目前
會把整欄的可見事件全部排版並渲染，沒有做 viewport 裁切）。

現值不寫在這裡 —— 每補一批就會過期。README 開頭那句是每次補資料都會一併更新的，
要查就查那裡（2026-08 為 518 則，離 2000 還很遠）。這份文件裡其他的數字多半是
**當時的實測紀錄**（例如標籤排版那幾張表、詳情面板遮蔽 15 則），那些是決策依據，
不要拿現況去「訂正」它們。

**`importance` 驅動縮放分層。** 全域視角只顯示 5，放大逐層釋出。
這是密度問題的主要解法。補資料時 importance 給得隨便，畫面就會糊掉。

**`base` 從 `GITHUB_REPOSITORY` 自動推導**（`vite.config.ts`）。
GitHub Pages 專案頁面掛在 `/<repo>/` 底下，`base` 設錯是整頁空白最常見的原因。

---

## 實作過程中踩到的坑

1. **`漢` 的 end 寫成 220，與 `三國` 的 start 220 重疊** —— `assertNoOverlap`
   在載入期就 throw 了（畫面全白，console 有明確訊息）。這個 guard 值得留著，
   補資料時會一直幫忙。已改成 219。
2. **標籤位移累積造成年代順序失真**（見上）。
3. **圖釘系統性低 11px**（見上）。
4. **React 的 `onWheel` 是 passive 的，攔不到 `ctrl+wheel`**，
   `preventDefault()` 無效。改成 `useEffect` 裡手動
   `addEventListener('wheel', …, { passive: false })`。
5. **暗色模式下引線幾乎看不見**（原本用 `--baseline`）。引線是「位移誠實性」的
   保證，不能低調到看不見，改用地區色 55%。
6. **軸範圍外的事件會靜默消失** —— 沒有任何錯誤，元素被畫到畫布上方而已。
   已補 `assertInRange`（見上方「時間軸的上下界」）。順帶抓到 `MAX_YEAR` 寫死
   2025 早已過期。**這類「畫面看起來正常，資料其實錯了」的 bug 是這個專案
   最該防的一種**，發現時優先補載入期防護，而不是只修當下那筆資料。
7. **正好落在 `minYear` 的事件，標籤上半列會被切掉。** 標籤以中心定位，
   年份 = `minYear` 時 `yearToY` 回傳 0，`top` 就成了負的，上半列跑到內容區外
   被 sticky 欄位標題蓋住（台灣「前3000 大坌坑文化」實測 `top: -11px`）。
   **修法不是把 `minYear` 往前調** —— 那只會把同樣的問題交給新的最早那一則。
   已在 `layout.ts` 夾住（`Math.max(y, HALF_ROW)`），照樣會畫引線指回真實年份。
   位移最多 13px，小於 `MIN_SHIFT_PX`（14px），不會害事件退化成只剩圖釘。

   注意 **`HALF_ROW` 有兩個不同的值**：`layout.ts` 是 13（半個 26px 列距，
   含列間空隙），`EventMark.tsx` 是 11（半個 22px 視覺高度）。名字一樣、
   用途不同，改的時候別以為是同一個。

---

## 已知限制與可能的下一步

- **超過四個地區時**要重新驗證相鄰欄的配色（見上方「顏色只承載地區」）。
  版面本身已支援任意數量，見「多地區版面」。**每個主題各自要驗** ——
  鐵道史刻意只切四欄（以營運者而非路線分欄）就是為了留在已驗證的範圍內。
- **沒有虛擬化**，見上方 DOM 渲染那條。
- **所有主題的 YAML 會打進同一個共用 chunk。** `import.meta.glob` 的 pattern
  必須是靜態字串，eager 之後 Rollup 沒辦法依 entry 切分。目前的量可以忽略。
  真的大到影響首屏時，作法是給每個主題一支自己的 entry module 明確 import
  自己的資料檔 —— **不要改成 `eager: false`**，那會讓資料變成 async，
  `MIN_YEAR` 就沒辦法留在模組層，「主題是 per-document 常數」那整套前提就沒了。
- **`tw-railway` 目前是種子資料**，只走過維基 API 的標題查證，還沒補到正式規模。
- **資料是 Claude 在對話中逐筆整理的**（擬候選 → 維基 API 批次查證 → 寫入），
  不是資料庫匯入，也不是逐字手打。大量匯入建議走 Wikidata SPARQL，授權 CC0
  可直接用。注意維基百科的**條目文字是 CC BY-SA**，採用敘述需標示出處；純日期
  與事實不受著作權保護。目前 `desc` 欄位皆為自行撰寫。
